{
  "name": "sayaqr",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "description": "Privacy-first offline QR code generator built with Vite and TypeScript.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "vitest": "^3.2.4"
  }
}
